# Dev journal — September 2026

Append-only, newest at the bottom (CODING_STANDARD §12). August lives in [2026-08.md](2026-08.md).

## 2026-09-01 — Fade/haze/blur become per-priority (Jorge's report, effect groups)

Jorge was right: fade and haze were whole-layer, keyed to P0's depth
alone (a sunken P1 never darkened; a sunken P0 tinted its in-zone P1
too), and blur selected per-tier but took the farthest tier's strength.

Fix - EFFECT GROUPS: tiers sharing a depth share one pass; when a
layer's tier depths split and any effect is on, the layer draws once
per depth group, each pass with ITS OWN fade/haze tint and ghost
strength, alpha-hiding the other groups. The masks partition the
tiles - nothing draws twice, color math stays exact. Extra passes only
in 3D with effects on and depths split; equal depths degenerate to the
single pass. The atmosphere refactored into
gpu3dsSetStereoAtmosphereForDepth(depth) (one depth's math) + the
layer wrapper; the ghost tier-on machinery was subsumed by the
partition and deleted. The draw block finally became a lambda
(drawLayerContent) shared by the group loop and the spotlight passes.

Validated on the pillar state (P0=0 in-zone, P1=-4 behind, FADE=8):
the pillar alone darkens, the P0 structures stay bright, the fill
keeps the seam continuous. The stale-object rule held: probe build
after a clean flip needed its own make clean.

## 2026-09-01 — #71 first round: merge-by-result groups + per-call fill math

Jorge's field data cracked the Butoden mystery open:

* The "haze that turns itself on" is the GAME's own flicker transparency
  (alternating layers every frame for the split-screen fog) - not our
  effect. What our side DID waste: with priorities split and any effect
  on, a layer paid one pass per DEPTH even when both depths sat inside
  the focus zone - two full section-heavy passes to apply a no-op tint.
  Groups now merge by the effect's RESULT (tint color + ghost params,
  computed by the new pure gpu3dsComputeAtmosphereForDepth): identical
  results share one pass. Butoden's split-screen case collapses back to
  a single pass.
* The fill width (with its slider read) was computed per TILE ROW inside
  the walkers - dozens of times per layer per frame on raster-heavy
  games, 2D included. Hoisted to once per walker call
  (S9xComputePriorityFillWidth), shared by both walkers.
* Validated in Azahar on the pillar state: the per-priority fade scene
  is pixel-equivalent pre/post merge (4% residual = the game's animated
  lights). Remaining for #71: hardware profiling of MMX3's 3D dips
  (fill quad volume is the suspect) and the Butoden savestates.

## 2026-09-01 — #71 round 2: shared-tint base passes

Jorge's retest: round 1 helped but blur still dropped frames on Butoden
(and his "Old" turned out to be 3DS Mode on the New - blur cannot exist
on a real Old, whose slider zeroes the whole stereo path). The "haze at
blur 0" is the game's own flicker transparency made visible by uneven
frame pacing under 3D load.

The remaining fat: with priorities split and blur on, the group loop
paid one BASE pass per group even when the groups' tints were identical
(blur does not tint) - 4 passes where 3 carry the result. Base passes
now merge by tint (union of the groups' dims) and only the ghost passes
run per ghost-parameter set, masked to their tiers. The draw lambda
decomposed into drawPass + drawGhostPasses primitives on the way.

Validated in Azahar on the pillar state, both branches: blur-only split
(shared base + P1 ghosts) pixel-matches the pre-optimization smear;
fade split (two tints, two bases) stays pixel-equivalent (4% residual =
the game's animated lights). Blur's inherent 3-pass cost remains - if
Butoden still drops frames, the next lever is an optional light blur
(single ghost).

## 2026-09-01 — Blur Quality: Light (issue #71, built ahead of Jorge's retest)

Option in the 3D Stereo tab's Settings (global, config v2.5): Full keeps
the two ghost passes; Light draws ONE ghost per frame, its side
alternating per frame and running opposite in the two eyes, alpha
boosted 1.4x (cap 0.55) so the perceived smear stays close to Full.
~33% off the blur's draw cost for section-heavy games.

Validated in Azahar on the pillar state: picker sets Light, consecutive
frames show the ghost on opposite sides (alternation live), no tearing.

Lesson (cost me three runs): hand-inserting a new key into a v2.4
settings.cfg derails the SEQUENTIAL parser at that line - every later
key (menu hotkey included) loads as default and gets SAVED BACK. Set
new keys through the menu, or write the whole file at the new version.

## 2026-09-01 — Numbers: Blur Full vs Light (issue #71), Azahar 2126.0

Jorge asked for numbers. No emulator times the 3DS GPU, so a
PROBE_DRAW_STATS build counts what the GPU is handed: draw calls and
vertices per frame, base vs ghost, averaged over 60 frames. MMX3 pillar
savestate, both eyes, BG1 split (P0=0 in zone, P1=+4 out), forced slider:

| scenario            | draws | vertices | ghost draws/verts |
|---------------------|------:|---------:|------------------:|
| A: 3D, blur off     |    14 |    5,849 |            0 / 0  |
| B: blur 6, Full     |    18 |    9,800 |        4 / 3,952  |
| C: blur 6, Light    |    16 |    7,825 |        2 / 1,976  |

Blur Full adds +4 draws / +3,952 verts (+68% of the frame's vertices)
for ONE split layer; Light adds +2 / +1,976 - exactly half the blur's
increment, ~20% fewer vertices for the whole frame. Each additional
blurred layer adds the same again, which is what a section-heavy split
screen multiplies.

Azahar 2126.0 installed (backup Azahar-2125.1.3.bak.app): its new
"Simulate 3DS GPU timings" (simulate_3ds_gpu_timings=true) turns the
Frame ms readout into a load indicator - A 7.07 ms, B 8.96 ms. Menus are
English now (language-detection fix); launch with
`open -a Azahar <path.3dsx>` (the direct binary pops a warning dialog).
It logs "Unimplemented reinterpretation RGBA8 -> D24S8" for the depth
texture written as a color target - an Azahar limitation, the pattern is
hardware-validated (the window prepass always did it).

## 2026-09-02 — Blur Quality Auto (issue #71)

Jorge: "compensa deixar o Full?" - sim, como look de referência; o Light é
válvula. A fusão dos dois é o **Auto**, agora default: Full enquanto o
frame cabe, Light quando o loop começa a pular frames, Full de volta após
janelas limpas.

- `source/3dsblurauto.h`: lógica pura, sem estado de plataforma. Janela
  de 60 frames; ≥2 frames pulados na janela → Light + cooldown de 3
  janelas; janela limpa queima uma; qualquer skip re-arma. 7 casos em
  `tests/test_blur_auto.cpp` (borda da janela, tolerância a 1 skip,
  re-arme, retorno).
- Sinal = o `skipDrawingFrame` ORIGINAL de `impl3dsRunOneFrame` (frameskip
  por carga), amostrado antes do pacing de FMV do MSU-1 mexer nele - vídeo
  em 30fps não é carga. Veredito em `GPU3DSExt.blurAutoLight`; o
  `drawGhostPasses` lê `quality==Light || (quality==Auto && verdict)`.
  Flip vai pro log (`[blur] auto -> light|full`) para o Jorge ver no
  hardware quando acionou.
- Config v2.6: renumerado 0=Auto, 1=Full, 2=Light. Arquivos v2.5 tinham
  `0` gravado como default implícito (Full) - a migração força Auto ao
  ler, então quem nunca escolheu ganha o Auto sem perder nada.
- Não é provável no Azahar (100% de velocidade, o frameskip nunca dispara);
  a lógica está coberta por testes e a fiação são três linhas. Validação
  real: hardware do Jorge no Butoden + log.

## 2026-09-02 — Aba 3D: "Settings" desce para cima de "Tools"

Jorge: o bloco Settings (Slider Response, Edge Cleanup, Blur Quality, Fill
Priority Gaps) ficava no topo da aba, antes dos perfis e das gauges - são
preferências globais, raramente tocadas; quem abre a aba quer as gauges. O
bloco desceu para logo acima de Tools. Zonas do idle tick
(s_stereoGaugeFirst, s_stereoFxFirst/Last) são gravadas em items.size()
na construção, então a mudança de ordem não as afeta.

Levantado também: o `.3d` é indexado pelo NOME DO ARQUIVO da ROM
(`Memory.ROMFilename`) - compartilhar exige o mesmo filename do outro
lado. Avaliação registrada na conversa; decisão pendente (nome interno do
header como chave + fallback legado).

## 2026-09-02 — .3d indexado pelo título do header, não pelo filename

Jorge: "escolher o arquivo 3D pelo nome pode ser ruim: o shareable fica
mais difícil" - correto. `stereo3d/<filename>.3d` só casa quando o outro
lado tem a ROM com o MESMO nome de arquivo; o conteúdo (profundidades,
perfis de cena por fingerprint) é da ROM, a chave é que era frágil.

- Chave nova = `Memory.ROMName` (título do header, já com espaços finais
  aparados pelo snes9x) sanitizado por `3dsstereokey.h`: trim, `\/:*?"<>|`,
  bytes de controle e não-ASCII viram `_`; sem letra/dígito → 0 e o
  chamador cai no filename como antes. 6 casos em `tests/test_stereo_key.cpp`.
- Um único resolvedor (`stereo3dGamePath(out, n, dir)`) alimenta load,
  save, Copy From, Backup e Restore; `stereo3dLegacyPath` só existe para
  migração.
- Migração: `stereo3dMigrateLegacy` - se `<título>.3d` não existe e
  `<filename>.3d` existe, copia (o legado fica no lugar) e loga
  `[stereo3d] migrated`. Vale para o `.stereo3d-bak` também. O Copy From
  esconde o legado do jogo atual para não aparecer duplicado.
- CRC32 descartado como chave: exato demais (revisão/hack/dump = outro
  arquivo, e o compartilhar morre de novo). Regiões e revisões de um jogo
  compartilham layout - compartilhar o .3d entre elas é o comportamento
  desejado.

## 2026-09-02 — Aba 3D: linhas não usadas escurecem; "Hide Unused Layers" opcional

Jorge perguntou se esconder camadas/prios sem uso seria bom. Decisão:
escurecer por default (lista estável, dá para editar BG4 pausado numa
tela sem BG4) e esconder só por opção consciente.

- `3dslayeruse.h` (puro, 5 testes): acumulador por frame + cópia
  latcheada no fim de um frame RENDERIZADO, para o editor nunca ver um
  frame pela metade nem um frame pulado (que marcaria tudo como sem uso).
- Marcações: as macros DRAW_*_BG_INLINE nomeiam a BG (`s_layerUseBg`)
  antes do walker; os dois tile inlines marcam `[bg][prio]`; o caminho de
  mosaico marca direto (não passa pelos inlines); sprites marcam
  `[4][Priority]`; Mode 7 marca a BG (EXTBG prio 1 na segunda passada).
  `S9xStartScreenRefresh` zera, `S9xEndScreenRefresh` latcheia após o
  FLUSH_REDRAW.
- Menu: gauges e checkboxes de camada ganham `TextColor` = 60% da cor de
  item desabilitado quando a linha não desenhou; o draw de Gauge e
  Checkbox passou a honrar `TextColor` (só usava em Action). Com Hide
  Unused (cfg v2.7, `StereoHideUnused`) as linhas somem e a aba é
  reconstruída; a zona do idle tick usa um mapa gauge→(layer, prio) em
  vez de índice fixo, então o spotlight continua correto com linhas
  ausentes.
- Validado no Azahar (intro do MMX3): Sprites, BG1 Prio 1 e BG2 Prio 1
  escuros; BG4 aparece usada - a intro é Mode 0 (4 BGs). Sem ROM
  carregada tudo conta como usado.

## 2026-09-02 — Escopo congelado para a v2.1

Jorge: "acho que é o suficiente para a nova versão". Escopo fechado: só
correções do teste em hardware entram até a tag. A investigação das
quedas de fps do MMX3 com 3D no New foi descartada por decisão dele
("mate o lance do MMX3") - não é bloqueio de release e sai da lista de
pendências. Roteiro de teste: Butoden com blur + log (`[blur] auto`),
pilar do MMX3 A/B vs rcmz, linhas escuras/Hide Unused em 3 telas,
`stereo3d/` migrando, update mid-game pela nightly seguinte.

## 2026-09-02 — Issue #72: Auto Save / Auto Load com a linha do tempo

Ideia do Jorge para depois da v2.1, com a ressalva "se não estragar meu
SD". Já existe `Auto Savestate` por jogo (grava .auto.frz na saída,
carrega no boot). A issue propõe: retomar com pergunta + thumbnail;
persistir os últimos segundos da fita do rewind na saída (opt-in) para a
timeline sobreviver ao fechar; rede de segurança só em eventos calmos
(menu, sleep, HOME), nunca por relógio por default; escrita atômica
(tmp + rename) e leitura validada. Números de desgaste registrados na
issue (~300 KB por estado; fita até ~24 MB só na saída).

## 2026-09-02 — Blur Auto: entrada imediata + histerese adaptativa (issue #71)

Feedback do Jorge após o teste: mais sensível para entrar no Light; só
voltar ao Full "com certeza"; e a janela poder crescer conforme a
quantidade de trocas. Nova regra em `3dsblurauto.h`:
- Entra no Light no PRIMEIRO frame pulado, no próprio frame (antes:
  2 pulos e só ao fechar a janela de 1 s).
- Volta ao Full após `required` janelas limpas seguidas (base 3 = 3 s);
  qualquer pulo no meio reinicia a contagem.
- `required` DOBRA (teto 60 = 1 min) a cada recaída em menos de 10
  janelas depois de voltar ao Full; recaída entre 10 e 30 janelas mantém
  o valor; 30 janelas estáveis em Full devolvem ao base. Um jogo na beira
  assenta no Light em vez de ficar piscando entre os dois.
- 9 casos de teste (entrada imediata, reinício, escalada 3→6→12→24→48→60,
  reset por estabilidade, faixa intermediária).

## 2026-09-02 — Issue #73: update com jogo aberto (malloc) + cursor do Hide Unused

Jorge relatou o erro de alocação ao atualizar com ROM aberta e lembrou que
o fluxo pedido era fechar o jogo e recarregar NA MESMA SESSÃO - o park de
#66 (retomar no próximo boot) era "meio auto save/load só para o update".
- Causa provável: o SOC buffer (1 MB alinhado) + curl são alocados NO
  CHECK, com rewind (~24 MB no heap), MSU-1 e cache vivos; heap
  fragmentado → memalign falha ou curl "Out of memory".
- Fix: `update3dsNetReserve()` no boot (buffer fica pela sessão);
  `emulatorUnloadRom()` (cheats/config salvos, mixer drenado, MSU
  RomUnload, rewind reset+finalize, isRomLoaded=false) antes do download;
  `emulatorResumeParked(rom)` recarrega ROM + `.update.frz` depois -
  também usado pelo `tryUpdateResume` do próximo boot. Log
  `[upd] heap before net` via mallinfo.
- Cursor após "Hide Unused Layers": a reconstrução da aba preservava o
  ÍNDICE; linhas escondidas acima empurravam o cursor. Agora reencontra a
  linha por (Type, Text) e reposiciona o scroll se ficou fora da janela.
  Vale para toda reconstrução (Copy From, perfis).
- Não validável no Azahar (sem rede). Reteste no hardware.

## 2026-09-02 — Blur por prioridade (de verdade) e refresh do Hide Unused

Jorge, no hardware: (1) Hide Unused não atualiza quando uma BG/prio
passa a ser usada; (2) o blur pega a BG inteira, não a prioridade.

- (2) Causa: o TexEnv do passe fantasma tira a alfa de
  `TEXTURE0 × constante` (para sobreviver a cenas com color math, que
  zeram a classe de alfa do vértice) - e a alfa do vértice é justamente
  onde a máscara de tier (dim por prioridade) vive. Os passes base
  respeitam a máscara; os fantasmas ignoravam e cobriam todos os tiers.
  Fix sem tocar no TEV: `drawGhostPasses(ghost, tierMask)` manda os tiers
  fora do grupo para x+4096 pela paralaxe por tier - o clipper descarta,
  custo zero. Validado no Azahar (probe slider, state do pilar, BG1 P1=+4
  fora do foco, BLUR=6 vs 0): diff de pixels = 0,0% no fundo (P0),
  5,9% no pilar e 1,8% no piso (P1).
- (1) Causa: `setupMenu` só reconstrói abas sujas; a aba 3D era montada
  na primeira pausa e ficava. Agora cada entrada no menu com ROM marca
  TAB_3D suja - as linhas escuras/escondidas descrevem a pausa atual; o
  cursor sobrevive pela identidade (type+text) do commit anterior.
- Truque de validação novo: `update-resume.txt` + `<rom>.update.frz`
  carregam ROM e state no boot sem nenhuma tecla (o robô de teclas do
  Azahar 2126 é intermitente).

## 2026-09-05 — Stable v2.1

Jorge testou a nightly 0b46e99 no hardware: "ficou muito bom. Passe para
stable". Corte: AppInfo 2.0 → 2.1; CHANGELOG ganha a seção v2.1 (a antiga
"Unreleased" vira "Stable v2.0"); ledger fecha #60/#61/#65/#66/#70/#71/#73;
branch empurrada para `stable` (workflow cria a release
stable-YYYYMMDD-<sha>, marcada latest, com as notas da seção Unreleased)
e para `master` (fast-forward). Depois da release: renomear a seção para
"Stable v2.1 (...)" e abrir uma "Unreleased" vazia; fechar as issues no
GitHub apontando para a release.

## 2026-09-05 — Auto Save / Auto Load (issue #72, escopo do Jorge) + warm-up do Blur Auto

Jorge fechou o escopo: uma opção para habilitar; salvar ao entrar no
menu, ao sair do emulador e ao "desligar"; carregar ao abrir o jogo.
- `AutoSaveLoad` global (cfg v2.8, Emulator tab > Save and Load, default
  off). Efetivo = global OU o `AutoSavestate` por jogo antigo
  (`impl3dsAutoSaveActive`).
- Gatilhos: `showMenu()` (menu aberto, mixer parado); hook APT
  ONSUSPEND/ONSLEEP (HOME e tampa - o último ponto antes de um
  desligar; o libctru chama o hook na thread principal dentro do
  aptMainLoop, fronteira de frame, então o snapshot é consistente); saída
  (já existia). `[autosave] <motivo>: saved` no log.
- Load: no ROM load, silencioso (o `.update.frz` do updater carrega
  depois e prevalece).
- A fita do rewind persistida ficou de fora (não pedida).
- Blur Auto: as primeiras frames após um load pulam por motivos que não
  são carga (caches, threads) e viravam Light por 1 s; `warmup` de 60
  frames ignorando pulos, reset a cada `firstFrame`. Teste novo.
- Lição: o log de sessão é nomeado pela versão (`debug_v2.1_session.log`);
  li o v2.0 antigo por vários minutos.

## 2026-09-05 — Harness de validação no Azahar (issue #69)

`tools/azahar-validate/validate.py` (Python stdlib, macOS): arma uma
cena no SD do Azahar (autoboot + `<rom>.update.frz` + `update-resume.txt`
= ROM e state carregados no boot sem tecla; `.3d` do jogo; overrides em
chaves EXISTENTES do settings.cfg), lança o 3dsx, pina a janela numa
geometria canônica, captura a tela de cima (400x236) e compara: contra
golden (`run`) ou entre duas cenas (`ab`), por região, em % de pixels
alterados, com máscara `.diff.pgm`. Restaura tudo e mata o Azahar com
kill -9 (saída limpa regravaria os arquivos sob teste).
- Cenas iniciais: `mmx3-pillar-blur6` vs `mmx3-pillar-blur0` (a prova do
  blur por prioridade de ontem, agora reproduzível; bandas esperadas:
  fundo P0 0..0.5%, pilar P1 ≥2%, piso P1 ≥0.5%). State versionado em
  `states/mmx3-pillar.frz` (421 KB).
- `docs/testing/hardware-checklist.md`: os 7 itens que só o console
  valida. CODING_STANDARD §13: prova de pixel obrigatória para mudanças
  que o Azahar enxerga; CI é quem publica.
- Pendente: a primeira execução real do `ab` (tela do Mac travada na
  hora; build probe pronta).

## 2026-09-05 — Harness: primeira execução real (issue #69)

`validate.py ab mmx3-pillar-blur6 mmx3-pillar-blur0` com a build probe
(-DPROBE_FORCE_SLIDER), tela destravada: total 1,55 %; fundo P0 0,00 %
(OK), pilar P1 5,86 % (OK), piso P1 1,81 % (OK) - os mesmos números da
prova manual de 02/09, agora em um comando, ~80 s, sem tecla. Aprendido
no caminho: com o display apagado o Azahar nem cria janela e o
`screencapture -R` falha ("could not create image from rect") - o
harness agora explica isso na mensagem de erro em vez de falhar mudo.

## 2026-09-05 — Auto save no Azahar: barrado pela trava de áudio (esperado)

Menu aberto no Azahar com AutoSaveLoad=1: o caminho rodou, mas
`impl3dsHasBrokenAudioStateSignature()` barrou a gravação e escreveu o
`.auto.frz.broken-audio.log` (DSP todo zero, FLG=60, KeyedChannels=0):
sem DSP firmware o Azahar não roda a APU, e isso É a assinatura de state
mudo que a trava existe para não persistir. No hardware a APU está viva.
Conclusão: auto save/load não é provável no Azahar; entra no checklist de
hardware (já estava, item 5). O log agora diz `[autosave] <motivo>:
skipped (broken-audio signature)` em vez de ficar mudo.

## 2026-09-06 — Blur Light: o fundo embaçado "mexia de um lado pro outro" (state do Jorge)

Savestate do Zero Project na represa (BG2 -3 embaçada = céu/floresta).
Reprodução no Azahar com slider forçado, 4-6 capturas a 0,3 s:
- **Light** (alternância de lado por paridade de frame): metade de cima
  variando 0 % / **6,56 %** / 0 % entre pares consecutivos - o fantasma
  saltando de lado, exatamente o "mexendo".
- **Full**: 0,00 % em todos os pares (controle).
- **Light corrigido** (um fantasma por olho, lados opostos nos dois olhos,
  fixo no tempo): 0,00 % em todos os pares; Light vs Full num mesmo frame
  3,98 % (um fantasma vs dois - esperado).
Causa: a aposta de que a persistência do LCD fundiria a alternância a
60 Hz estava errada para fundos detalhados; a fusão agora é binocular.
Vale para o Auto sob carga, que é onde o usuário via isso sem escolher
Light. Harness ganhou `frames` + `temporal_expect` (pior diff entre
frames consecutivos por região) e a cena `mmx3zp-dam-light` com o state
versionado; a cena reproduz a sequência manual acima e passa a rodar a
cada build probe.

## 2026-09-06 — Crash na saída pós-update (data abort, FAR=0x44) + MSU-1 mudo na retomada

Jorge: update com jogo aberto (nightly do auto save → be33575), ao
responder "sair" → exceção ARM11 core 0, data abort de LEITURA em
FAR=0x44 (NULL + 0x44), PC 003634B0, LR 00363D8C, SP 08C2F970 (pilha de
thread alocada no heap, não a principal). Na retomada seguinte, o MSU-1
ficou mudo até o jogo mandar tocar de novo (morte).

Pilha lida da foto: frames ativos contêm `alttp_msu-24.flac` - o `char
path[]` local de `msu3dsAudioReadaheadTick` - logo a thread que morreu é
a de read-ahead de áudio do MSU-1, dentro de um tick, na saída. A
simbolização exata falhou: nightly já sobrescrita e o layout local não
bate com o CI (ordem de objetos por wildcard difere entre FS; 1,3 KB de
diferença), mesmo com a mesma imagem docker (digest igual), mesmo
caminho e libcitro3d refeita. Lição: guardar o .elf de cada release no
CI (a fazer) e pedir o .dmp do Luma (traz o código em volta do PC).

Hipóteses cobertas por hardening (todas baratas):
- `log3dsWrite` não era thread-safe e a thread de read-ahead loga via
  msu1_diag enquanto a principal loga e fecha o arquivo na saída
  (check-then-fprintf em FILE sendo fechado): um LightLock em volta de
  todo uso, re-check sob o lock, close sob o lock.
- `snd3dsFinalize` ignorava o resultado do `threadJoin` de 1 s e
  liberava ring + decoder embaixo de uma thread ainda viva: agora só
  libera se o join retornou; senão loga e deixa vazar.
- MSU-1 mudo: `emulatorResumeParked` carregava o `.update.frz` FORA da
  janela de drain do mixer; o evento SavestateLoaded do MSU-1 limpa a
  fila de áudio e não pode correr com o fill. Agora
  drain → load → resume, como o Load Slot do menu e o auto load por jogo.

Pendente: o .dmp do Jorge para confirmar a função exata; e ELF por
release no workflow.

## 2026-09-06 — Crash pós-update: causa confirmada pelo .dmp do Luma

O `.dmp` chegou mangled (binário decodificado como Latin-1 e regravado em
UTF-8; `decode('utf-8').encode('latin-1')` recupera os 1924 bytes;
header Luma v1.3: magic DEADC0DE/DEADCAFE, regs 92 B, code 96 B, stack
1680 B). Desassemblando os 96 bytes antes do PC:

    ldr r2,[r4]            ; dirp->device = 3
    ldr r3,=devoptab_list
    ldr r1,[r3,r2,lsl #2]  ; devoptab_list[3] == NULL
    ldr lr,[r1,#0x44]      ; ->dirnext_r  -> FAR 0x44

É o `readdir` da libsysbase com o dispositivo `sdmc:` já removido. Quem
faz readdir fora da thread principal é `file3dsBgScanThread` (varredura
em segundo plano do navegador, detached); a string `alttp_msu-24.flac`
na pilha é a listagem da pasta do ALTTP MSU que ela percorria. Cadeia:
descarregar o jogo em sessão (#73) → aba Load Game volta → varredura
dispara → usuário sai → `main` retorna → libctru desmonta o sdmc → a
thread cai no próximo readdir. A hipótese anterior (read-ahead do
MSU-1) estava errada; o hardening feito continua válido.
Fix: `bgScanAbort` checado no laço de readdir, `file3dsBgScanShutdown()`
(abort + espera até 3 s) no início de `file3dsFinalize`, e nenhuma
varredura nova depois do abort. Lição: thread detached que toca o SD
precisa de ponto de parada na saída; e o .elf por release (já no CI)
teria poupado a tarde.

## 2026-09-06 — Auto Save / Auto Load visível antes de carregar ROM

Jorge: "olha direito: não aparece" - as duas preferências globais (Auto
Save/Auto Load, screenshot ao salvar) estavam dentro do bloco CURRENT
GAME da aba Emulator, que só existe com ROM aberta; o Auto Load precisa
ser ligado ANTES de abrir o jogo. Movidas para uma seção global
SAVESTATES, antes de REWIND. "Save and Load" no bloco do jogo fica só
com os slots.

## 2026-09-06 — Retomada de MSU-1 deixava o navegador dentro da pasta do pack

`emulatorResumeParked` (e o autoboot) faziam `SetCurrentDir(pasta da
ROM)`; para um pack MSU-1 isso entra na pasta que o navegador trata como
entrada virtual única ("Pack/rom.sfc" a partir do pai) e nunca abre. Novo
`emulatorPointBrowserAt(romPath)`: se a pasta da ROM é pack
(`file3dsIsMsuPackDir`, wrapper público do probe), diretório = pai e
romFileName = "Pack/rom.sfc", como uma escolha no navegador; senão o
comportamento antigo. lastSelectedDir/Filename passam a persistir certo.

## 2026-09-06 — Stable v2.2; diário de setembro separado; branches

Jorge: "mova para Stable, atualize toda a doc, limpe as branches". Corte
v2.2 (minor: Auto Save/Load é feature). As entradas de setembro que eu
vinha anexando ao 2026-08.md foram movidas para este arquivo. Branches
no origin: só master, nightly, stable e feature/rewind-delta - a última
é o tronco de desenvolvimento com nome obsoleto; master avança por
fast-forward a cada stable, então o tronco passa a ser o próprio master
e feature/rewind-delta é apagada. Próximo: Mode 7 em 3D (#62).

## 2026-09-06 — Mode 7 em perspectiva (issue #62): núcleo entregue

Jorge: "vamos começar o 3D mode 7", com dois modos (por camada e Mode 7)
chaveados por tela. Descoberta que simplificou tudo: as scanlines do
Mode 7 já passam pelo shader dos tiles (o `shader_mode7` só monta a
textura), então a perspectiva é um fator por vértice de linha + uma `mad`
no shader - e o "chaveamento por tela" é por scanline, de graça: o fator
só existe em vértices de Mode 7.
- `3dsmode7persp.h` (puro, 5 testes): w = 256·spanMaisPróximo/span,
  clamp 8..256; fator = 1 + k·(w/256 − 1).
- gfxhw: as linhas são enfileiradas e emitidas após o span de referência
  (a linha mais próxima) ser conhecido; vértice de linha vira SVector4i
  (`{SHORT,4}` no atributo); flag "Mode 7 desenhou" latcheada por frame.
- Shader: uniforme `mode7Persp` (#9), constantes num temp (picasso não
  aceita constante/negativo como src3 de mad). k só na camada de Mode 7
  (`vboId == VBO_SCENE_MODE7_LINE`), 0 nas outras = no-op exato.
- `.3d`: `M7PERSP` 0..8 (default 8), por perfil; gauge "Mode 7 >
  Perspective" na zona de preview ao vivo, escurece se o frame pausado
  não desenhou Mode 7.
- Probes novos: `PROBE_SBS` (dois olhos lado a lado numa captura) e log
  `[m7persp]` (rampa por run de linhas).
Números (Super Mario Kart, demo em tela dividida, Azahar, slider forçado):
`rows=83 spanRef≈55 w top=9 bottom=256` em todos os frames - rampa
monotônica do horizonte à linha de baixo; no logo do título (mapa plano)
não há rampa. A métrica de disparidade por correlação ficou ruidosa no
chão amarelo liso (Mario Circuit) - prova visual + log substituem.
Pendências: benchmark no Old (gate), teste no hardware (é o que o olho
julga), cantos com Edge Cleanup.

## 2026-09-06 — Mode 7: faixa do céu sumia no hardware (fila vs commit)

Jorge (Mario Kart 2P no aparelho): "um layer sumiu". No Azahar a região
aparece azul (o Mode 7 do Azahar é o bug conhecido do amarelo), no
hardware a pista cobria o céu. Causa: em `RepeatTile0` a seção era
commitada ANTES do `m7FlushLines()`, então as linhas do "tile 0" (fora do
mapa = céu) eram emitidas depois do commit e caíam no draw seguinte.
Flush antes do commit. Lição: fila de vértices + commit por seção = o
flush tem de ser a última coisa antes de cada commit, nos dois passes.
Também vi que o vértice direito da scanline (y = -16384) pula o bloco de
paralaxe no vertex shader: a linha estica em vez de transladar (era
assim no plano também). Melhoria separada, fora deste hotfix.

## 2026-09-06 — Mode 7: efeitos por distância e mais profundidade

Jorge, no hardware: (1) fade/haze/blur cobriam a pista inteira por igual;
(2) "dá pra aumentar a profundidade".
- Uniforme `mode7Persp` virou (k, gain, -fog, -ghost). A gauge Perspective
  0..8 se divide: 0-4 rampa (4 = completa), 5-8 ganho nas linhas próximas
  (8 = 2×) - `mode7PerspGaugeSplit`, testado.
- Névoa por linha: o vertex shader escreve `fog·(1 − w/256)` no canal
  azul da cor do vértice (para scanline é 0; nos outros draws z=0 → soma
  0) e um TexEnv variante interpola pela cor primária (SRC_B) em vez da
  alfa da constante. Cor = mistura preto/haze pelo share, como o modelo
  por camada; `mode7FogAmount` testado.
- Blur por linha: os passes fantasma do plano usam o shift base e o
  offset entra pelo uniforme w como `ghost·(1 − w/256)` - zero na linha
  mais próxima, cheio no horizonte (antes o fantasma seguia o shift:
  borrava mais perto, o oposto).
- `M7FX` no .3d (default 1), checkbox "Effects by Distance" no bloco
  Mode 7. Lições do picasso: em `mul`, uniforme só como src1; em `mad`,
  src3 tem de ser temporário.
- Azahar não valida (Mode 7 amarelo); prova no aparelho.

## 2026-09-06 — Perspective acima de 4 mostrava bordas

Jorge: "o máximo que fica legal é 4; a partir daí aparecem bordas". O
Edge Cleanup recorta pelo maior |depth| entre as camadas
(`stereoMaxAbs`), e o ganho das linhas próximas (gauge 5..8, até 2×) não
entrava na conta. Agora a BG1 (o plano) contribui com |depth|·gain para
o maxAbs; a gauge do Mode 7 é publicada antes do ApplyValues. Log `[m7]`
no menu para o caso do bloco escondido (aguarda a linha do Jorge).

## 2026-09-06 — EXTBG respondia à gauge errada; bloco Mode 7 conferido no Azahar

Jorge: "a BG está errada". As macros dos tiles setam a fronteira de
prioridade (`stereoPrioZBoundary[bg]`) a cada frame; a macro do Mode 7
nunca setou - a segunda passada da BG2 (EXTBG, depth 8) caía no tier 0 e
movia com a gauge BG2 Prio 0, enquanto o menu marcava a Prio 1 como usada.
Fix: BG2 com fronteira entre 2 e 8; BG1 estacionada. UX: gauge "Above the
plane (EXTBG)" no bloco Mode 7 (mesmo valor da BG2 Prio 1); o bloco só
existe quando o jogo usa Mode 7 (draw no último frame OU PPU em modo 7
na pausa); recorte de borda conta o ganho da perspectiva (5..8).
Validação no Azahar (Mario Kart, corrida 2P pausada): `[m7] menu:
drawnLastFrame=1 bgMode=7 bg1used=1` e o bloco "Mode 7 > Perspective"
visível na aba. A métrica de pixels L/R do PROBE_SBS não serve em cena
em movimento: o olho direito vem um frame atrás (render alternado) -
usar sinais de log ou cenas paradas.

## 2026-09-06 — Mode 7: um plano, uma gauge (EXTBG como deslocamento)

Jorge (irritado, com razão): "tá pegando a BG errada". Com EXTBG o plano
do Mode 7 é desenhado como BG1 mais BG2 com split de prioridade por
pixel; no Mario Kart o bit está no ASFALTO, então a BG2-1 movia a pista e
a BG1-0 movia a grama - duas gauges para metades de um plano só. Regra
nova no draw: no Mode 7, os dois tiers da BG2 seguem a profundidade da
BG1; os pixels de prioridade somam (BG2 P1 − BG2 P0), 0 por default. A
gauge do bloco Mode 7 virou "EXTBG offset". Validação: quatro runs
pausados no Azahar (probe SBS, cena parada = olhos no mesmo frame), uma
gauge a +6 em cada, medindo quais pixels diferem entre os olhos - pendente
da tela do Mac destravar (o vigia roda sozinho). Lição (de novo): não
declarar pronto pelo código; a prova é a captura ou o aparelho.

## 2026-09-06 — Regra do plano único revertida (decisão do Jorge)

"Essa layer pode mudar de jogo pra jogo? Se sim, apenas arrume o hide."
Sim: o EXTBG é sempre BG2 com split por pixel, mas QUAIS pixels carregam
o bit é do jogo (Mario Kart: o asfalto). Revertida a amarração BG2→BG1;
as gauges ficam absolutas (BG1-0 plano, BG2-1 pixels de prioridade), o
bloco Mode 7 repete a BG2 Prio 1 como "Priority pixels", e o Hide mostra
as linhas que o frame desenhou (BG1-0, BG2-1 no Mario Kart), com o bloco
Mode 7 garantido pelo registro do PPU.

## 2026-09-06 — Preview do editor 3D: Trim → Zoom bugava a tela de cima

Pedido do Jorge: análise completa do "ficar mostrando as PRIO/BG".
Causa raiz: o menu redesenha a tela do jogo em UMA passada (um buffer do
double buffer), enquanto o preview do editor desenha os DOIS. Depois de
um preview, qualquer mudança que usava o caminho do menu (Edge Cleanup
Trim → Zoom via menu3dsSetScreenDirty) caía num buffer só e a tela
alternava entre o crop velho e o novo. Achados corrigidos:
1. Edge Cleanup, Blur Quality e Slider Response agora usam
   stereo3dRestorePausedLook (dois buffers), como o Fill Priority Gaps.
2. O preview marca gameScreenBufferDesync: um redesenho do menu depois
   dele (Scaling, slider) roda duas passadas.
3. O idle tick roda ANTES do check de slider do menu: mover o slider com
   o cursor numa gauge desenhava o preview e, no mesmo frame, o pause por
   cima (spotlight sumia). menu3dsGameScreenPresented() cancela o
   redesenho pendente e sincroniza o IOD lembrado.
4. showMenu não zerava o rastro do preview: reabrir o menu com o cursor
   na mesma gauge não redesenhava o spotlight (mesma chave, mesmo slider).
Validação: testes (172) e boot da cena MMX3 no Azahar; a paridade dos
buffers só aparece no aparelho - Jorge confirma.

## 2026-09-07 — Mode 7: âncora nova revertida (percepção > geometria)

A nightly 104d1ec trocou a âncora: gauge = linha de perto, horizonte
afunda além dela com qualquer sinal. No Azahar a geometria provou
(state do Jorge, BG1 −6: 6 half-px chato, 12+ nas linhas longe com
Perspective 8). No aparelho, Jorge: "não via nenhuma sensação de
profundidade; parecia só uma extensão do BG". Explicação: o plano
inteiro ficou atrás da BG1 e o horizonte foi a 14 px de disparidade -
fora da zona de fusão confortável o gradiente some e vira "camada mais
funda". O modelo antigo mantém o horizonte no plano da tela, onde a
fusão é fácil, e o gradiente lê como chão inclinado - com gauge
negativa a geometria é "invertida" no papel, mas é o que o olho
prefere no 3DS (afundar não briga com a borda da tela). Revertido o
código; ficam o medidor SBS corrigido (metades da janela, 128 px) e as
cenas do Mario Kart. Lição: um argumento geométrico não vale uma
olhada no aparelho.

## 2026-09-07 — Sprites seguem o chão do Mode 7 (#76, ideia do Jorge)

Pergunta do Jorge: "tem como os itens em cima do mode 7 terem o 3D
mudando com a distância?" Desenho: automático por posição (o pé do
sprite dentro das linhas do plano) com exceções marcadas no editor por
assinatura tile+paleta, e duas gauges (Ground Near / Far). Por dentro:
VBO próprio dos sprites com posição de 4 shorts (w = linha + 256·slot),
tabela por linha preenchida no flush do Mode 7 e lida no frame
seguinte, tabela de assinaturas do frame, uniform indexado no shader
(flag de chão + alpha do spotlight) armado só no draw dos sprites.
Chaves .3d: GSPR, GNEAR, GFAR por perfil; GROUNDX por jogo. Cenas do
harness: smk-race-ground0/1 (linhas do kart do jogador: 0 vs near).

## 2026-09-07 — #76: personagem partido em profundidades → clusters

Jorge (Near 0 / Far −2 no Mario Kart): "problemas no recorte dos tiles";
sugeriu UI para unir tiles com um tile mestre. Causa: um personagem são
vários sprites de hardware (kart, piloto, sombra), cada um pegava a
linha do próprio pé. Solução sem UI: union-find nas caixas visíveis que
se tocam (2 px), o grupo inteiro segue o pé mais baixo e um slot só
(o do pé, ou o de um membro marcado). Efeito colateral bom: a lista do
editor encolheu de 22 para 8 no state do Mario Kart e o spotlight acende
o personagem inteiro. Tela do Mac travada na hora da medição; o log
`[ground] armed ... sigs=8` confirma o caminho.

## 2026-09-07 — Mode 7: vértice direito com profundidade (técnica do rcmz)

Pendência antiga: o vértice direito da scanline é o marcador do geometry
shader (y = −16384) e não decodificava plano, então caía sempre no tier
0; o esquerdo (y = linha + depth) podia cair no tier 1 quando as duas
prioridades da BG tinham gauges diferentes - a linha esticava. Agora o
direito carrega −16384 + depth (o geometry shader detecta por limiar,
y projetado < −1, então continua marcador), decodifica o mesmo plano e
devolve o marcador puro. `mode7RightVertexY` testado; build e boot no
Azahar OK.

## 2026-09-07 — Listras no console (nightly 102a86a): alpha no marcador

Foto do Jorge: o plano do Mode 7 virou listras verticais. O y do vértice
esquerdo é (alpha << 13) + depth·256 + linha, e o helper copiou tudo acima
da linha para o marcador do vértice direito: com alpha 16384 o y virou
positivo, deixou de ser marcador e o geometry shader desenhou cada
scanline como tile. Azahar não mostrou porque a tela do Mac estava
travada e a captura não rodou - o boot "sem erro" não vale como prova
visual. Correção: só o nibble de profundidade (y & 0xF00) vai ao
marcador; testes cobrem os três valores de alpha.

## 2026-09-07 — Captura com a tela travada: o emulador salva o próprio framebuffer

A tela travada do Mac bloqueou a prova visual três vezes hoje (e deixou
passar as listras). Tentativa 1: `azahar -d video.webm` pela linha de
comando - o app sobe mas não roda o 3dsx nem grava (diálogo atrás do
lock, provavelmente). Tentativa 2, que ficou: `-DPROBE_FBDUMP` faz o
emulador salvar a tela de cima apresentada em probe_top_600.png no SD
(mesma dança do screenshot: PPF, desfaz o swap, invalida, salva 400x240)
e `--fbdump` no harness espera o arquivo. Resultado: quatro cenas do
Mario Kart medidas com o Mac travado - pista chata 6 px, perspectiva
crescendo 4,5 px, sprites 0/3 px - e a pista inteira na imagem, sem
listras (prova visual da correção do vértice direito). O filtro de
bandas com textura passou a usar o spread entre os deslocamentos
testados: um sprite sozinho sobre o plano amarelo casa perfeito (resíduo
0) mas tem spread claro; o plano vazio tem spread 0.

## 2026-09-07 — Old 3DS: F-Zero bom, Mario Kart lento

Jorge não usa blur no Mode 7 (não gostou) e não vê diferença entre Full
e Light - não há mesmo: um fantasma por olho em lados opostos funde no
mesmo borrão que dois. Duas ações: (1) Blur Auto começa em Light e sobe
para Full só com a taxa provada (a primeira promoção não conta como
retorno, senão um skip logo depois dobraria a prova); (2) EXTBG: a
passada baixa da BG2 é exatamente os pixels da BG1 (índice < 128, mesmo
tilemap, mesma matriz) e fica coberta por ela - pulada quando BG1 desenha
no mesmo segmento com a mesma máscara de janela (predicado puro,
testado). Surpresa medida no Azahar: o Mario Kart NÃO entra no EXTBG
(nem na corrida 2P do state, nem na demo 1P aos 60 s - o log `[m7]
extbg` nunca aparece; a "BG2 Prio 1 usada" vinha da metade de baixo da
tela em modo normal). Então o skip serve a Tiny Toon / Contra III /
Castlevania IV, não ao Mario Kart, e ficou sem prova visual por falta
de ROM EXTBG no SD - o predicado é exato por construção (pixels
idênticos sob a BG1) e o log diz no aparelho se aplicou. O custo do
Mario Kart no Old 3DS segue sendo emulação (DSP-1 + dois planos por
frame pela tela dividida); o 3D acrescenta um composite por frame. A
dupla de medidas 2D vs 3D no console decide se há algo do 3D a cortar.

## 2026-09-07 — Aba 3D sem parágrafos; Ground Lift intertravado (#75/#76)

Jorge: "as explicações estão poluindo" e "Near/Far é o mínimo e máximo
do plano? Se Far −3 e um obj em −4, ele fica atrás do chão?". Sim, era
um defeito de desenho: Near/Far eram profundidades soltas, sem relação
com o plano. Agora o sprite herda a profundidade do plano na linha do pé
(BG1 × ganho na linha de baixo, × (1−k) no horizonte - a mesma rampa
das scanlines) mais um Ground Lift 0..3 para a frente; não existe mais
configuração impossível e mexer na BG1/Perspective arrasta os sprites.
UI: 33 linhas de texto viraram ajuda por item em SELECT (o campo
PickerDescription que os pickers já usavam; o loop do menu ganhou o
gesto); bloco Mode 7 na ordem pedida (Effects by Distance, Sprites
Follow the Ground, Ground Lift, sprites da tela, Perspective, Plane
Depth = BG1 P0 tirada do Depth; Priority Pixels = BG2 P1 só com EXTBG);
Tools virou um diálogo com as seis ações. A lista de sprites ficou
inline (o spotlight ao vivo depende do idle da aba, que diálogos não
rodam). Scene Profiles ficou inline e compacta pelo mesmo motivo do
picker aninhado.

## 2026-09-08 — Kart pulando de profundidade na derrapagem

Jorge: "ao derrapar o sprite muda de distância de forma bruta" e "não
entendi o lift". Causa: a profundidade vem da linha do pé; no pulo o
sprite sobe alguns px, ou a sombra se separa e o pé vira o corpo do kart
- a linha muda num frame e a profundidade salta. Correção: rastreador por
personagem (assinatura + x/32) que desliza a linha usada até a linha
real a 6/255 por frame (`groundSlew`, `GroundTrack`, testados); saída do
plano continua imediata. Lift explicado como "quanto o sprite flutua
acima do chão" e padrão 0. Sem nova prova no Azahar: a cena é parada e
o primeiro avistamento não desliza, o resultado estático é idêntico.

Jorge: "Tools: pode voltar com as opções lá kkk" - o diálogo das seis ações era um passo a mais; voltaram como linhas, cada uma com ajuda no SELECT.

## 2026-09-08 — Ainda bruto na batida: identidade por posição + sombra

O deslize por assinatura não bastou: na batida/derrapagem a animação
troca a linha da folha de tiles, a assinatura muda, o rastreador via um
"personagem novo" e recomeçava sem deslizar. Agora o rastreador casa
por posição do pé (raio 24 px, entradas reivindicadas uma vez por
frame), passo 4/255. E o kart no ar sobre a sombra usa a linha da sombra
(grupo logo abaixo com sobreposição horizontal, vão ≤ 24 px) - é onde o
chão está. Tudo em funções puras testadas (190). Prova estática no
Azahar inalterada por construção; a dinâmica é no console.

## 2026-09-08 — "Pular foi o único que ficou bom": a fumaça roubava a memória

Derrapagem e batida têm sprites novos colados no kart (fumaça, faíscas).
Nascem a poucos px do pé e, casados um a um na ordem dos sprites,
pegavam a memória de posição do kart; o kart recomeçava do zero e
saltava. Casamento global por distância (`groundTrackAssign`: pares
mais próximos primeiro, cada memória uma vez por frame), testado com o
cenário exato. O pulo já estava bom pela regra da sombra.

## 2026-09-08 — "Ficou bom não": era o frame skip

Três correções no rastreador e nada. Lendo o hook de frame: FrameStart
roda TODO frame emulado, FrameEnd só nos renderizados. Num frame
pulado o plano não desenha, a tabela de linhas fica vazia, e o start
seguinte trancava prev = vazio → todos os sprites "fora do chão" (gauge
normal de sprite) por um frame, depois de volta ao chão: um salto por
frame pulado - o Old 3DS pula frames no Mario Kart. Correção: prev só
trava quando o acumulador tem linhas; a memória do rastreador envelhece
por frame desenhado. Probe de campo: `sdmc:/3ds/snes9x_3ds/groundprobe.txt`
liga `[groundprobe]` a cada 4 frames (kart mais baixo: alvo, usado,
memórias). Lição: derrapar/bater/pular eram sintomas; a pergunta certa
era "o que muda entre um frame e outro no caminho inteiro".

## 2026-09-08 — Probe no Azahar: a preparação rodava 5x por frame; pé = sprite maior

Jorge está no New (frame skip fora). Experimento com teclas (B segurado,
L, esquerda) e o probe `[groundprobe]`: as linhas saíam a cada frame com
um print a cada 4 chamadas → S9xDrawOBJSHardware roda por SEGMENTO
(0-23, 24-106, 114, 115-218, 223) sempre com todos os sprites; cada
chamada depois da primeira registrava os mesmos personagens como
memórias novas (mem=16-24) e alvo == usado em toda linha: o deslize nunca
aconteceu no aparelho. Agora: uma vez por frame (flag zerada no
FrameStart), memórias envelhecem no FrameEnd. Jorge: "na terra acontece
a mesma coisa" → poeira: pé do grupo = sprite MAIOR (corpo do kart), não
o mais baixo; sombra abaixo só se for achatada (≤ 8 px), poeira não
passa por chão. Rerun: kart do jogador estável em 241, deslizes
visíveis nos outros (232>238). 192 testes.

## 2026-09-08 — O log do console: pé na faixa sem plano + memória trocada

Jorge mandou o `[groundprobe]` do New. Kart do jogador em 127,101 alvo
238-243 o tempo todo - até a batida/derrapagem: o sprite muda de forma,
o pé vai para as linhas 108-112 (faixa entre pista e mapa, sem linhas do
plano) e sai `127,110:0>0`: fora do chão → profundidade normal de sprite
→ salto. E aos 13,8 s `127,101:239>131`: um item que passou pelo ponto do
kart deixou memória ali e o kart deslizou meio segundo da linha do item.
Correções: `groundRowNear` (última linha de plano acima, até 16) e custo
de casamento com (Δlinha/4)². Testes com os dois cenários do log. Cinco
tentativas por dedução antes; uma pelo log. Pedir o log primeiro.

## 2026-09-11 — SELECT help em todas as abas

A correção anterior estava restrita à aba 3D Stereo. Auditoria de todos
os construtores encontrou oito linhas Textarea explicativas e uma linha
Disabled de logging ainda em Emulator, Settings e Controls. Migradas
para PickerDescription nos controles correspondentes; os três checkboxes
de palette deferral recebem ajuda individual. Mantidos somente os textos
de status (build/versão e ausência de cheats), além dos separadores.

Teste host de regressão protege os vínculos e a ausência dessas linhas:
197 casos / 7473 assertions passaram. Build 3DSX limpo passou. Criado
PROBE_MENU_HELP, compilado fora de releases, e cena menu-help.json:
resume marker carrega Super Mario Kart, entra em pausa e percorre nove
controles pelo caminho real de SELECT/B. Não altera valores dos controles.

Validação encontrou dois problemas no próprio capturador: o helper de
screenshot do jogo espera RGB8, mas o menu é RGB565; e o back buffer do
menu ocioso pode conter a tela anterior. O probe agora converte RGB565
explicitamente e redesenha ambos os buffers antes da captura. As primeiras
capturas foram descartadas como evidência. O runner restaura os arquivos
de SD preparados e encerra apenas o processo de teste que abriu. Azahar
2126 exige confirmar seu aviso inicial de execução direta; a navegação
dos menus não depende de teclas automatizadas.

Rerun final: nove listas e nove diálogos capturados e inspecionados;
listas sem explicações soltas, help legível sem corte. Diff A/B entre
lista e diálogo na região menu_body (0,30)-(320,220), por caso 0..8:
61.15%, 61.25%, 59.06%, 59.83%, 58.79%, 58.85%, 58.87%, 58.87%, 59.70%.
Esses números comprovam a mudança de tela, não equivalência com um golden;
a conferência visual complementa o teste estrutural. Capturas finais em
tools/azahar-validate/goldens/menu-help/. Publicação solicitada em nightly,
via build limpo do CI sem flags PROBE; desempenho no console não foi medido
porque esta mudança não altera o caminho de emulação.

## 2026-09-11 — #78/#79, primeira rodada de implementação (validação em andamento)

Baseline bbef7fa. SRAM agora verifica escrita completa e close, preserva dirty
em falha e restaura silêncio anterior. BufferedFileWriter mantém erros até o
close; savestates em arquivo também propagam falha. Ainda é I/O síncrono e não
há troca atômica do arquivo: falha pode deixar conteúdo parcial no SD.

FreezeStruct/UnfreezeStruct passam a reutilizar workspace limitado pelo maior
tipo serializado no ABI alvo, zerando o mesmo comprimento do formato antigo.
Isso remove seus new[]/delete[], não a alocação interna do fmemopen nem todos
os custos de rewind. Propriedade permanece no thread do core, com rewind sob
snesAccessLock; não foi introduzido worker de gravação.

OAM: campos só de aparência não invalidam listas por scanline; flush continua
antes da alteração, VFlip/posição/tamanho/rotação mantêm invalidação. Mode 7:
referência por mínimo ao quadrado e efeitos por distância somente em Direct.
Release passa a exigir citro3d custom como os targets individuais.

Host: 208 casos / 139411 assertions, também sob ASan+UBSan, passaram. Criado
runner de frame fixo e cenas Layer M7FX 0/1. PROBE_FBDUMP também grava/recarrega
um estado no frame 600; flags de probe nunca entram na publicação. Compilações
ARM locais lentas motivaram CI de validação em next-development, somente com
artifact e sem release. A prova visual e a publicação ainda estão pendentes
neste registro; resultados serão acrescentados após execução.

#63: verificado cherry-pick 738dbc5 de ef11ac0; fxemu.cpp/fxinst.cpp/fxinst.h
idênticos. Rodada original já feita; follow-up de Star Fox continua aberto.
Não fechar #78/#79 com esta rodada parcial ou atribuir ganhos de console sem
medições Old/New. Escopo entregue e pendências em testing/optimization-78-79.md.

### Resultado da validação — 2026-09-11/12

O CI encontrou includes transitivos ausentes (testes no GCC e API de paths
no memmap); corrigidos. O primeiro teste da versão candidata no Azahar pegou
uma regressão: open exigia o write buffer também para leitura, mas settings
é lido antes dessa alocação. Isso descartava configurações iniciais. Corrigido
para exigir buffer só em modo gravável, com teste explícito de leitura sem
buffer. O runner também passou a retirar/restaurar logs antigos para não
aceitar evidência de outra execução. Capturas anteriores da candidata foram
descartadas. Nada dessa rodada intermediária foi publicado em nightly.

Versão validada b893227; CI ARM 34665296669 verde. Host final: 208 casos /
139414 assertions, incluindo ASan+UBSan. Baseline bbef7fa e candidata usam
os mesmos três flags de probe e o mesmo hook de save/load sob lock.

Azahar 2126, frame 600, comparação RGB exata (além do threshold do harness):

| Cena / comparação | Região | Pixels diferentes |
|---|---|---|
| SMK Direct, baseline → candidata | tela 400×240; jogo (72,8)-(328,232) | 0% |
| SMK Layer, baseline → candidata | tela 400×240 | 0% |
| MMX3 blur6, baseline → candidata | tela; city_bg_P0; pillar_P1; floor_P1 | 0% em todas |
| SMK Layer, candidata M7FX=0 → M7FX=1 | tela 400×240 | 0% |

Savestates gravados no frame 600 são byte a byte iguais antes/depois nas
três cenas baseline/candidata, e foram recarregados com sucesso. SHA-256:
SMK (Direct e Layer) b458b810568e354b373232d0b026295a4a84a5835041690574734edd6521aed7;
MMX3 572a9d092e37c395a421861c9728f88c2bd7d255cdf72a2196b883b68d6531aa.
Isso cobre os blocos dessas duas ROMs; não é prova de todos os chips especiais.
Capturas versionadas em tools/azahar-validate/goldens/optimization-78-79/.

ELF confirma C3D_FVUnifMaxDirty ligado e workspace de 65.596 bytes em BSS
(64 KiB + 60), sem alocação por FreezeStruct/UnfreezeStruct. O custo residente
é explícito; não foi alegada redução de RAM ou ganho de FPS. Rewind ainda usa
fmemopen no load. Publicação desta rodada será em nightly limpa, sem probes;
as issues permanecem abertas para os outros itens e medições Old/New.

## 2026-09-12 — #78, logs em lotes (validação em andamento)

O caminho anterior fazia três escritas formatadas e fflush por mensagem sob
lock compartilhado com produtores MSU. Mantida a formatação, mas o sink agora
usa buffer próprio fixo de 32 KiB, flush por janela de 1s, por capacidade ou
no close. Gameplay e loop interativo do menu bombeiam a janela com TryLock.
Não é worker assíncrono: o esvaziamento continua síncrono e pode atrasar quando
a execução fica suspensa/bloqueada. Crash pode perder a cauda recente do log.

Initialize/close continuam no main thread. Flag atômica publica o lock pronto;
FILE e timestamps ficam inteiramente sob esse lock, sem leitura concorrente
de LogFileEnabled pelos workers. A configuração passa a valer no restart,
como a ajuda do menu já informa. Retornos de escrita/flush/close são tratados;
falha desabilita o sink sem recursão de log. Nenhuma alteração em SRAM assíncrona
ou proteção transacional dos saves foi incluída nesta rodada.

Host macOS: 211 testes / 141972 assertions passaram. Os novos casos verificam
999 mensagens sem flush antecipado, tick sem mensagem nova, rollback de relógio,
close/reopen, saída maior que o buffer e argumentos inválidos. Linux adiciona
falha real de SD simulado com /dev/full. CI ARM e teste integrado ainda pendentes.

Validação concluída para 3b41724: CI ARM 34668324297 verde; Linux 212 testes /
141984 assertions (incluindo /dev/full), macOS 211 / 141972 também sob
ASan+UBSan. /dev/full testa falha de escrita do sistema de arquivos, não emula
latência nem os modos físicos de falha de um cartão SD.

Azahar 2126: SMK Direct frame 600 com logging ativo, log de roundtrip efetivamente
descarregado. RGB exato comparado ao golden optimization-78-79/final-direct.png:
0% de pixels alterados na tela 400×240. Savestate idêntico à referência:
SHA-256 b458b810568e354b373232d0b026295a4a84a5835041690574734edd6521aed7;
recarga bem-sucedida. Nenhuma alteração de render ou de formato de save.

Evidência de custo removido é a ausência de flush antecipado para 999 mensagens
pequenas no teste, seguida de descarga por tick. Não é benchmark Old/New.
Logging desligado não ganha desempenho com esta etapa. Publicação solicitada
em nightly limpa; #78 continua aberta, inclusive para SRAM assíncrona/segura,
coalescência de assinaturas, demais custos de rewind e profiling em hardware.
